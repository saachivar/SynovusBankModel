// The frontend timeout before showing the "still processing" message.
// The prompt specifies a closing time of 14 seconds. We set our watchdog lower.
export const WATCHDOG_TIMEOUT_MS = 7000; // 7 seconds